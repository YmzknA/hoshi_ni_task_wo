<div class="w-full h-full">
  <!-- 月の画像 -->
  <%= render 'title_header', title: 'メール認証' %>

  <!-- 内容表示部分 -->
  <div class="z-10 space-y-5 bg-base-300/30 border-t-1 border-x-1 border-base-200 relative p-8 min-h-[90vh] text-center rounded-t-[4rem] xl:text-xl">
    
    <!-- フラッシュメッセージ -->
    <%= render 'flash' %>
    
    <div class="card bg-base-300 w-full max-w-xl mx-auto p-8">
      <div class="text-center mb-6">
        <div class="mb-4">
          ⭐⭐⭐
        </div>
        <h2 class="text-2xl font-bold mb-2">認証メール再送信</h2>
        <p class="text-base-content/70 text-sm">
          認証メールが届いていない場合は、<br>
          下記フォームから再送信できます
        </p>
      </div>
      
      <%= form_with model: resource, as: resource_name, url: confirmation_path(resource_name), 
                    method: :post, local: true, class: "space-y-6" do |f| %>
        <%= render "error_messages", resource: resource %>
        
        <div class="form-control">
          <%= f.label :email, "メールアドレス", class: "label label-text font-medium" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", 
                           value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email),
                           class: "input input-bordered w-full focus:input-primary",
                           placeholder: "example@example.com" %>
        </div>
        
        <div class="form-control">
          <%= f.submit "認証メールを再送信", class: "btn btn-primary w-full" %>
        </div>
      <% end %>
      
      <div class="divider my-6"></div>
      
      <div class="text-center space-y-3">
        <p class="text-sm text-base-content/70">
          メールが届かない場合は以下をご確認ください
        </p>
        <div class="text-xs text-base-content/60 space-y-1">
          <p>・迷惑メールフォルダをご確認ください</p>
          <p>・メールアドレスに間違いがないかご確認ください</p>
          <p>・認証メールの有効期限は24時間です</p>
        </div>
      </div>
      
      <div class="mt-6 text-center">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
  
  <!-- メニューバー -->
  <%= render 'menu_bar' %>
</div>