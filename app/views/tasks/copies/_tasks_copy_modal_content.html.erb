<div class="modal-box w-sm max-h-[90vh] max-w-[85dvw] px-2">
  <div class="flex w-full items-center justify-center">
    <div class="glasscard card z-10 flex h-2xl w-full justify-center p-5 px-0 md:px-5 text-center md:w-2xl">
      <div class="flex items-center justify-center text-secondary mb-3">
        <p class="text-xl">タスクをコピーする</p>
      </div>
        <div class="flex h-full items-center justify-center mb-8">
          <p class="text-sm text-base-200">チャートからコピーする場合は、<br>再読み込みするまで表示されません。</p>
        </div>

        <%= form_with model: @task, url:tasks_copies_path(id:@task.id) , method: :post, local: true, data: { action: "submit->loading#show" } do |f| %>
      <div class="flex justify-center">
        <label class="mb-8 w-50">
          <span class="text-base-200 text-sm">始点となる日付</span>

          <%= f.date_field :start_date,
            autocomplete: "Start Date",
            placeholder: "開始日",
            min: @task.milestone&.start_date,
            max: @task.milestone&.end_date,
            class: "text-base-200 bg-base-100 rounded-full p-3 w-full" %>
      </label>
      </div>

      <%= f.submit "コピー", class: "btn btn-primary text-base-100 px-9" %>
    <% end %>
    </div>

  </div>
      <div class="modal-action flex justify-center">
      <form method="dialog">
        <button class="btn btn-accent px-9">
          <span class="material-symbols-outlined">
            close
          </span>
        </button>
      </form>
    </div>

</div>
